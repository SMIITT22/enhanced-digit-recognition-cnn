# =========================
# requirements.txt (CPU)
# =========================
# Python 3.8+ compatible pins known to work well with TF 2.13
tensorflow==2.13.0
numpy>=1.22,<2.0
matplotlib>=3.5
pillow>=9.0
opencv-python>=4.7
scikit-learn>=1.1
requests>=2.28
h5py>=3.8
# TensorFlow 2.13 is more stable with protobuf < 4.25 on some platforms
protobuf>=3.20,<4.25

# --- Optional (quality of life) ---
tqdm>=4.64

# --- Notes ---
# 1) Do NOT add 'keras' separately; TF 2.13 already bundles compatible Keras.
# 2) tkinter is part of Python stdlib. On Linux you may need the OS package:
#    Ubuntu/Debian: sudo apt-get install python3-tk
# 3) For NVIDIA GPU support, use a matching TF build and CUDA/cuDNN toolchain.
#    See: https://www.tensorflow.org/install/pip  (choose versions for your GPU)

# =========================
# How to install & run
# =========================
# Create & activate a virtual env (recommended)
# python -m venv .venv
# Windows: .venv\Scripts\activate
# macOS/Linux: source .venv/bin/activate

# Install deps
# pip install --upgrade pip
# pip install -r requirements.txt

# Run the app
# python digit_AI.py

# =========================
# Troubleshooting quick tips
# =========================
# • If matplotlib backend errors: it should auto-use TkAgg via FigureCanvasTkAgg;
#   ensure python3-tk is installed (Linux).
# • If EMNIST download times out, the app already falls back to MNIST only.
# • If training is slow, lower batch size (e.g., 32) or reduce epochs.
